import React, { useEffect, useState } from 'react';

const NotesIcon = ({position, id, closeModal}) => {
  const [ details, setDetails ] = useState({})
  return(<div></div>)
}

export default NotesIcon;